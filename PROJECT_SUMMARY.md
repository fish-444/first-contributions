# 알로카시아 온실 스마트팜 프로젝트 — 정리 문서

> 이 문서는 사람이 읽고, **다른 LLM(ChatGPT·Gemini 등)에 그대로 붙여넣어 이어서 작업**할 수 있도록
> 쓴 핸드오프 문서입니다. 프로젝트 배경·구조·기술·다음 단계를 한 파일에 담았습니다.

- **저장소**: `fish-444/first-contributions`
- **브랜치**: `claude/yolo-leaf-detection-webapp-qe6sd1`
- **사용자**: 코딩 비전공, **알로카시아(Alocasia)** 를 온실에서 재배
- **온실 스펙**: 공간 60×40×50 cm, 그로우 라이트 3개(정면 기준 왼쪽 2·오른쪽 1), 화분 약 15개

---

## 1. 프로젝트 한 줄 요약

식물 사진 속 **잎을 YOLO로 탐지·계수**하는 것에서 출발해 →
**로보플로우 모델 학습** → **3D 온실 디지털 트윈** → **빛 효율 분석** →
**격자 배치 플래너**까지, 코딩 없이 쓸 수 있는 웹앱 5개를 만들었다.

---

## 2. 만든 웹앱 5개

| 앱 | 위치 | 스택 | 실행 | 하는 일 |
|---|---|---|---|---|
| 잎 탐지 웹앱 | `leaf-detector/main.py` + `static/index.html` | FastAPI + YOLO | 서버 | 사진 업로드 → 잎 탐지 → 박스 표시 |
| 설치 없는 잎 탐지 | `leaf-detector/standalone.html` | 순수 HTML/JS | 더블클릭 | 브라우저가 로보플로우에 직접 호출 |
| 3D 디지털 트윈 | `leaf-detector/greenhouse-3d.html` | Three.js | 더블클릭 | 3D 온실 + 빛 효율 슬라이더 |
| 격자 배치 플래너 | `leaf-detector/greenhouse-grid.html` | Three.js | 더블클릭 | 96칸 격자 배치 + 알로카시아 + 빛효율 |
| 스마트팜 대시보드 | `smartfarm/` | FastAPI + YOLO + Three.js | 서버 | 분석→3D갱신→클릭 모달, 센서+RAG |

> Three.js는 오프라인에서도 열리도록 각 파일에 내장(또는 `static/`에 동봉)되어 있음.

---

## 3. 백엔드 아키텍처 (스마트팜 대시보드 `smartfarm/main.py`)

**흐름**: 사진 업로드 → YOLO 분석 → 가상 센서 → RAG 피드백 → 상태 저장 → 3D 반영 → 클릭 모달

**분석 엔진 자동 선택**:
1. `ROBOFLOW_API_KEY` 있으면 → 로보플로우 서버리스 추론
2. `.pt` 파일(`MODEL_PATH`) 있으면 → 로컬 ultralytics
3. 둘 다 없으면 → 데모(가짜 결과, 즉시 체험 가능)

**YOLO 박스 → 지표 가공**:
- **크기 분류**: 잎 면적 비율(coverage) + 잎 개수 → 대품/중품/소품
- **총 잎 개수**: 잎 박스 수
- **겹친 잎(overlapping)**: 잎 박스끼리 IoU > 0.12 인 것들
- **새순(new shoot)**: `newleaf` 클래스 or 유난히 작은 잎(면적 중앙값의 40% 미만)

**가상 IoT 센서**: 온도(구역별 경향+노이즈), 빛 효율(아래 4번 계산값)

**RAG 피드백**: 규칙+문장 지식베이스(`KNOWLEDGE`)에서 현재 상태에 맞는 항목을 검색·조합
(예: 새순 발생, 잎 겹침 과다, 빛 부족, 고온, 대품 분갈이 권장 등)

**API**:
- `GET /api/plants` — 전체 배치+상태
- `GET /api/plants/{id}` — 개별
- `POST /api/analyze` (form: `plant_id`, `file`) — 분석·저장 후 상태 반환

---

## 4. 빛 효율 계산 로직 (핵심 공식)

조명 3개 위치 `LIGHTS = [(-22,47,14), (-22,47,-14), (22,47,0)]` (x, 높이 y, z, 단위 cm).
각 화분 지점이 받는 빛:

```
illuminance(x, z) = Σ_lights [ cosθ / d² ]      (거리² 반비례 + 입사각)
  d   = 조명까지 거리
  cosθ = (조명높이 - 캐노피높이) / d              (위/잎 방향으로 들어오는 정도)
```

- 전체 화분 중 **가장 밝은 자리를 100%로 정규화** → 각 화분의 빛 효율(%)
- **균일도(uniformity) = 최소/최대 × 100** (높을수록 골고루) — "빛 효율 극대화"의 핵심 지표
- 격자 플래너에는 조명 콘(각도) 밖이면 급감(×0.15)하는 항도 포함
- 검증됨: 왼쪽 조명 옆 B1=100%, 먼 구석 A4/C4≈65%

---

## 5. 로보플로우(Roboflow) 모델

- **워크스페이스**: `s-workspace-br86f`
- **프로젝트**: `find-leaf-and-object` (및 `find-leaf-mcfh8`), object-detection
- **모델 ID(추론용)**: `find-leaf-and-object/1` (형식 = 프로젝트/버전)
- **엔드포인트**: `https://serverless.roboflow.com/{model}` (구 detect.roboflow.com)
- **인증**: 반드시 **Private API Key** 사용 (Publishable 키는 서버리스 추론에서 401)
- **클래스**: `leaf`, `newleaf`, `object`
- **학습 결과**: 라벨 383개로 학습, 잎 95%+ 탐지 확인

**연결 트러블슈팅(해결됨)**:
- 모델 칸에 워크스페이스명(`s-workspace-br86f`) 넣으면 안 됨 → `find-leaf-and-object/1`
- 공개키 대신 개인키
- 최신 주소 serverless.roboflow.com
- 탐지 0의 진짜 원인 = 학습 데이터 부족(8장) → 라벨 추가로 해결

**⚠️ 보안**: 과거 코드에 API 키가 커밋되어 공개 기록에 남음 → **키 재발급 필수**
(app.roboflow.com/settings/api → Regenerate). 절대 채팅/코드/깃에 개인키를 넣지 말 것.

---

## 6. 격자 배치 플래너 (`greenhouse-grid.html`)

- 바닥 60×40 → **5×5cm 격자 = 12×8 = 96칸**
- **배치 모드**: 칸 클릭 → 선택 크기(소품 1×1 / 중품 2×2 / 대품 3×3) 화분
- **다중선택 모드**: 여러 칸 선택 → "선택칸에 배치"로 임의 크기(멀티셀) 화분
- 화분 **자유 추가/제거**(클릭·Delete·전체 지우기), 겹침 방지(점유 격자 추적)
- **잎 개수 사용자 지정** 슬라이더(1~16): 새 화분에 적용 + 선택한 화분만 실시간 변경
- **알로카시아 3D 모델**: 잎자루(petiole) + 화살촉/하트 잎(ShapeGeometry) 여러 장이 위·바깥으로
- 화분 색 = 빛 효율(빨강 부족 → 초록 충분), 균일도 % 표시
- 정면/측면/탑뷰 카메라 프리셋

---

## 7. 다음 단계 (백로그)

- [ ] 격자 배치 ↔ 스마트팜 대시보드 통합 (한 화면에서 배치 + YOLO 분석)
- [ ] 화분 크기별 차지 칸 수 실제 지름에 맞게 보정 (소품 ~8cm 등)
- [ ] 자동 최적 배치 (빛 균일도 최대가 되는 화분 배치 제안 — 최적화/탐색)
- [ ] 대시보드를 실제 로보플로우 개인키로 실제 분석 모드 전환
- [ ] 잎 크기도 사용자 지정, 화분 이름/메모, 배치 저장/불러오기(localStorage)
- [ ] Spline·Blender로 만든 알로카시아 모델(.glb/.gltf)을 앱에 삽입
- [ ] RAG 피드백을 알로카시아 특화(겹침 솎기·광량·분갈이 타이밍)로 확장

---

## 8. 다른 LLM에게 (이어서 작업 시 참고)

- 사용자는 **코딩을 모름** → 실행법을 한 단계씩, 전문 용어는 풀어서 설명할 것.
- 배포는 정적 HTML(더블클릭) 또는 로컬 FastAPI 서버 위주. 개인키는 환경변수로만.
- 3D는 Three.js(r128). 새 3D 모델은 .glb 로 받아 GLTFLoader 로 교체 가능.
- 빛 효율은 위 4번 공식이 단일 소스 오브 트루스.
- 작업 결과는 항상 위 브랜치에 커밋·푸시.
