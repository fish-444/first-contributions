pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
opencv-python-headless>=4.8

# 백엔드(server/) — **정적 뷰는 이게 없어도 돈다.** 심사위원이 서버를 못
# 띄워도 dashboard/*.html 을 브라우저로 열 수 있어야 하므로, 서버는 선택이다.
fastapi>=0.110
uvicorn[standard]>=0.27
httpx2                  # fastapi.testclient 가 요구
