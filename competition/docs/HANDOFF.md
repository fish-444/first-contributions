# 인계 — 새 세션에서 이 프로젝트를 이어받을 때

> **스냅샷 문서다.** 수치의 정본은 [`STATUS.md`](STATUS.md) 이고, 여기 수치는
> 전부 거기서 온다. `tools/check_docs.py` 가 대조하므로 손으로 고치면 테스트가
> 깨진다 — 코드가 바뀌면 이 문서도 같이 갱신하거나 지워야 한다.

**마감 2026-08-31** · 저장소 `fish-444/first-contributions` ·
브랜치 `claude/yangdon-ai-competition-3x2ukm` · 최신 커밋 `e4bae1a`

**한 줄**: 발정을 놓치면 번식이 무너진다. 그런데 번식만 잡아도 농장은 안 산다 —
발정 탐지에서 출하까지 한 줄로 잇는다.

**규모**: 모듈 68개 · 대시보드 뷰 22개 · 테스트 78개 · 외부 연결 0

---

## 0. 먼저 알아야 할 것 셋

### ⚠️ 저장소가 공개다

```
fish-444/first-contributions   public    ← 우리가 커밋해 온 곳
fish-444/competition           private
fish-444/non-competitive       public
```

사용자는 이전 세션에서 이 저장소를 **비공개로 알고 계셨고**, 그 전제 위에서
"농장 식별자 원자료 커밋 금지 규칙을 바꾸겠다" 고 답하셨다. **전제가 틀렸다.**
공개 저장소에 농장 식별자가 올라가면 git 이력에 남고 회수가 불가능하다.
규칙을 손대기 전에 이 사실부터 사용자와 다시 확인할 것. 원자료를 꼭 버전관리해야
한다면 `fish-444/competition`(비공개)이 이미 이 세션의 소스로 붙어 있다.

### ⚠️ Google Drive 커넥터가 채팅에서 꺼져 있었다

`ListConnectors` → `connected: true` / **`enabledInChat: false`**.
계정 인증은 살아 있는데 대화에서 도구가 로드되지 않는 상태다. 코드로 켤 수
없고 UI 토글이라, 사용자에게 켜 달라고 요청하는 것 외에 방법이 없다.
새 세션에서는 켜져 있을 수 있으니 **먼저 `ListConnectors` 로 확인**할 것.

### 🔴 API 키 재발급이 아직 안 됐다 (과제 #6)

AI Hub · 케글 키가 이전 대화 기록에 노출됐다. **커밋 이력 전체 검색 0건**이라
이력 재작성은 불필요하고, 브라우저에서 재발급만 하면 된다 — 사용자 몫이다.
`.gitignore` 가 `.aihub_key` · `.env` · `*.key` 를 막고 있는 것은 확인했다.

---

## 1. 지금 상태

```bash
python competition/tests/smoke_test.py      # 78/78 통과
python competition/tools/check_docs.py      # 불일치 0
git status                                  # clean
```

---

## 2. 최근 세션에서 한 일 — 커밋 5개

| 커밋 | 내용 |
|---|---|
| `7be990e` | 농장 등록 돈사 섹션을 **출하까지** 확장 · 화면↔시뮬레이터 단절 4건 수정 |
| `2e76fb6` | **방향 역전** — `capacity_from_rooms()`. 돈사 → 받을 수 있는 두수·병목 |
| `ff194f0` | **생산 상한** — `throughput()`. 연간 출하 상한과 거기까지 가는 길 셋 |
| `4f26a9c` | `run_farm --setup` — 등록 JSON 이 ①②③ 를 전부 끌고 간다 |
| `e4bae1a` | `CAPABILITIES.md` — 할 수 있는 것 / 없는 것 목록 |

자세한 발견은 `STATUS.md` 의 **⑧′ ⑧″ ⑧‴ ⑨** 절에 있다. 요지 넷:

- **답은 두수가 아니라 병목의 이름이다.** 300두로 설계한 구성이 실제로는
  295두이고, 붙잡고 있는 건 임신사다. 임신사를 넓히기 전엔 다른 돈사를 키워도
  두수가 안 늘어난다.
- **방이 상한을 다시 깎는다.** 복당 이유두수 목표가 12두라도 자돈사 396자리에
  분만틀 36개면 11.0두가 천장이다. 목표를 그냥 상한으로 쓰면 **방이 못 받는
  생산량을 "낼 수 있다" 고 말하게 된다**(그렇게 허수 594두·1.4억원을 낸 적 있다).
- **연간 출하 = 분만틀 × 채움률 × 복당 이유두수 × 육성률 × 연간 배치수.**
  앞의 둘은 지어 놓은 것이라 성적으로 못 바꾸므로 **길이 정확히 셋**이다.
- **잡은 오류 8건 중 5건이 "잘 돌아간다" 고 거짓 보고할 뻔한 종류였다.**
  전이 0회를 "위반 0건 = 정상" 으로 본 것이 대표다.

---

## 3. 발행한 웹페이지 둘 — **저장소 미반영**

| 페이지 | 성격 | URL |
|---|---|---|
| 발정에서 출하까지 | 현황 문서 | `claude.ai/code/artifact/ce874aff-2a24-4a13-8c9d-c21cdec98d02` |
| 돈사 용량계 | 현장 도구 | `claude.ai/code/artifact/6f427302-f9ac-46dc-a791-447618b58fd9` |

용량계의 계산 로직은 `batch_flow` 에서 옮긴 것이고 브라우저에서 파이썬 모듈과
**8건 교차 대조해 전부 일치**했다(4개 구성 × 성적 유무 2가지). 다만 그 검증은
스크래치패드 일회성이라 **상수가 갈릴 수 있는 자리가 열려 있다** — `batch_flow`
값을 바꿔도 Artifact 는 모른다.

저장소에 23번째 뷰로 넣으려면 셋이 필요하다:
`build_barn_gauge.py` 빌더(상수를 파이썬에서 주입) · `smoke_test` 에 교차 검증
편입 · `check_docs` 뷰 카운트 갱신.

---

## 4. 남은 과제

| # | 할 일 | 메모 |
|---|---|---|
| 🔴 6 | API 키 재발급 | 사용자 브라우저 작업. 코드 쪽 완료 |
| ⬜ 11 | 발표 자료 → **보고서 형식** | 사용자가 "다음 주부터" 진행 예정. 아래 사전 조사 참고 |
| ⬜ 10 | 개체 이력을 ③단계 두수에 배선 | 방은 실제화 완료, 두수는 아직 유도값 |
| ⬜ 12 | 제출 잠그기 | 테스트·수치 동결 · 유출 점검 |

### #11 사전 조사 — Task 0 까지만 하고 멈춘 상태

- 현재 슬라이드 **14장 · 말하는 시간 합 10분 35초**. 발표 조건(시간·매수·Q&A)은
  문서 어디에도 적혀 있지 않다 — 보고서 형식이면 이 제약 자체가 사라진다.
- **⑥′ 는 이미 슬라이드 9 에 반영돼 있다**(`36053f7`). 원래 프롬프트의 Task 3 은
  불필요하다.
- **미반영 발견**: ③′ 분포 · ③″ 부트스트랩과 argmax 판단 · A-3 검출 한계 18%p ·
  처방 순서표 · ⑧ ⑧′ ⑧″ ⑧‴ · 원자료 중복 77.6%
- **철회 구조 2건**: 좌/우 상한(슬라이드 8 에서 세우고 8 안에서 또 12 에서 두 번
  거둠) · 누수 0.642(8 과 12 중복)
- **댕글링 참조 1건**: 슬라이드 9-1 이 "슬라이드 8 의 정지 프레임은 천장" 을
  인용하는데 그 문장은 슬라이드 13 에 있다.
- 슬라이드 6 지렛대 표는 틀린 게 아니라 `run_farm` 과 **다른 전제**다
  (PSY 24·육성률 86% vs 프로그램 가정 27.69). 보고서에선 전제를 나란히 밝히면 된다.

---

## 5. 진행 중이던 것 — AI Hub 71763

사용자가 **298MB**(TL.zip 265MB + VL.zip 33MB, 71763 라벨 전체)를 PC 에 받아
두셨고, 컨테이너로 옮기는 데서 막혀 있다.

### 확인된 사실

| 항목 | 결과 |
|---|---|
| 컨테이너 → AI Hub API 조회 | ✅ 파일 트리 정상 (298MB 구성 확인) |
| 컨테이너 → AI Hub 다운로드 | ❌ **"인증실패, 권한이 거부되었습니다"** |
| 디스크 여유 | ✅ 12GB — 압축 해제까지 넉넉 |
| 채팅 첨부 한도 | 30MB → VL.zip 33MB 도 안 들어감 |
| 로컬 폴더 마운트 | ❌ `/mnt/user-data/working` 은 컨테이너 자체 디스크, 브리지 아님 |

다운로드 거부 사유는 셋 중 하나다: 키 만료 / 71763 활용신청 미승인 /
국내 IP 전용. 셋째라면 어떤 키로도 이 컨테이너에서는 안 된다.

### 준비돼 있는 코드

`parse_aihub.parse_71763()` 파서 · `model_71763.py`(호흡수·현열량·잠열량 회귀,
개체 GroupKFold) · `docs/SCHEMA.md` 에 예상 스키마. 라벨 디렉터리를 안 주면
문서 스키마대로 합성 라벨을 만들어 파이프라인만 시연한다.

**단, `SCHEMA.md` 에 "실제 필드명이 문서와 미세하게 다를 수 있다" 고 적어 뒀다.**
그래서 **실제 스키마 확인이 첫 단계**다.

### 이 데이터가 맡을 역할

지금 `repro_cause_attribution` 과 `model_gilt_anestrus` 는 환경·사양 요인을
**가정 가중치**로 돌린다. 71763 이 들어오면 그 가중치를 지도학습으로 바꿀 수
있다. 다만 챔버 실측이라 **실농장 실증(구멍 #6)과는 다른 얘기**다.

### 제안했던 경로 — 사용자 미결정

1. **스키마 출력만 붙여넣기** (전송 0) — PC 에서 스크립트 돌려 출력만 받는다
2. **JSON 20개 채팅 첨부** (수백 KB) — 파서까지 실제 파일로 검증 가능
3. `fish-444/competition`(비공개)에 푸시 → pull
4. Drive 토글 켜기

작은 표본으로 먼저 감사하는 순서를 권했다 — 71471 때 라벨이 알고 보니 카메라
라벨이었던 걸 뒤늦게 발견한 적이 있다.

---

## 6. 지켜 온 규율 — 흔들지 말 것

- **등급을 문장마다 붙인다** — 실측 / 계산 / 유도 / 합성. 같은 표에 섞지 않는다
- **합치지 않는다** — 항이 곱해지는 지표는 "다 하면 +N두" 를 만들지 않는다
- **개입 효과를 주장하지 않는다** — 격차의 분해까지만. 실농장 개입 실험은 없다
- **빈 칸을 중앙값으로 채우지 않는다** — 격차가 늘 0 으로 찍혔던 버그
- **부정 결과를 앞에 놓는다** — 뒤에서 철회하지 않는다
- **원자료·가중치 미커밋** — `*.xlsx` · `*.pt` 차단
- **외부 영상은 제출물·앱에 넣지 않는다** (저작권)
- **수치는 코드가 정본이다** — `check_docs.py` 가 문서와 대조한다

## 7. 바로 돌려 보기

```bash
python competition/tests/smoke_test.py                       # 78/78
python competition/tools/check_docs.py                       # 불일치 0
python competition/src/build_farm_setup.py                   # 등록 화면 생성
python competition/src/run_farm.py --setup my_farm.json      # 여섯 단계 전체
python competition/src/barn_watch.py --setup my_farm.json --sweep
python competition/src/psy_priority.py --sows 300            # 처방 순서
```
