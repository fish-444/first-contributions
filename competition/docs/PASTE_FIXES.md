# 제안서에 넣을 것 — 복붙용 최종 묶음

> 블록마다 **어디에** 넣는지 적었습니다. 블록 안 텍스트를 그대로 복사하세요.
> 기준 판본: v6 (행동 모델 자체 학습 확인 반영).

---

## ① p11 「학습모델 표」에 행 하나 추가

탐지·자세·행동 행 아래에 이 행을 추가:

```
행동 분할 | Mask R-CNN 15클래스 | 622 폴리곤 라벨 | 처음 보는 200장(ts06) | bbox mAP 0.205 / segm 0.192
```

그리고 표 아래에 각주 한 줄:

```
행동 분할의 학습 보고값 0.953은 학습·검증이 같은 자료라 인용하지 않고, 처음 보는 200장에서 잰 값만 쓴다. 신뢰 클래스는 Resting·Eating·Walking·Searching 넷이며 이 밖의 출력은 쓰지 않는다.
```

**⚠️ p12~13 성능표(3.2.3)에는 넣지 마세요** — 학습모델 표(p11)에만.
외부 영상 구성비의 "신뢰 클래스 넷"이 어디서 왔는지가 이 행으로 설명됩니다.

---

## ② p15 외부 영상 절 — 본문 끝에 세 문장 추가

"...분모가 끝까지 따라갔다." 문장 뒤에:

```
이 관통에 쓴 탐지·행동 모델은 전부 본 과제에서 학습한 것으로, 제3자 사전학습 모델을 빌려 쓴 구간이 없다. 다만 관측 기록이 없어 판정의 적중 여부는 측정하지 않았으며, n=1 농장·개입 없음이므로 이 확인은 실증이 아니라 동작 확인이다. 추적은 요건에 미달했다 — 밀집 군사에서 트랙이 2.1~5.4초로 갈라져 개체 단위 누적이 불가능했고, 방 단위 집계까지만 성립한다.
```

---

## ③ [별첨] 자료출처 — 모델 가중치 표 (확정판)

```
■ 모델 가중치 — 세 가중치 모두 본 과제에서 직접 학습(제3자 모델 차용 없음)

가중치                         | 학습                                      | 성능(검증 방식)
pig_yolo.pt (YOLOv8n 1클래스)  | 자체 학습 · 45,611 bbox 통합              | mAP50 0.659 (학습 외 영상 동작 확인)
end2end.onnx (Mask R-CNN 15클래스) | 자체 학습 · AI Hub 622 폴리곤 라벨 ·
                               | mmdet 3.3.0 → mmdeploy 1.3.1 변환         | bbox mAP 0.205 / segm 0.192 (처음 보는 200장, 622 ts06)
resnet18 (자세 3·5클래스)       | ImageNet 사전학습에서 미세조정 (Kaggle T4) | 0.732 / MF1 0.534 (뷰별 LOVO 7폴드)

※ 행동 분할의 학습 보고값 0.953은 train==val 상태의 값이라 인용하지 않는다.
```

---

## ④ [별첨] 자료출처 — 데이터 표 (URL 채움용 뼈대)

p16~17에 시작하신 링크 표의 나머지 행. **조회일만 직접** 넣으세요:

```
데이터명                        | 링크                                                        | 조회일
소·돼지 발정행동 데이터 (71471) | https://www.aihub.or.kr/aihubdata/data/view.do?dataSetSn=71471 | ____
지능형 축사 통합 데이터 (622)   | https://www.aihub.or.kr/aihubdata/data/view.do?dataSetSn=622   | ____
양돈 생체 에너지 데이터 (71763) | https://www.aihub.or.kr/aihubdata/data/view.do?dataSetSn=71763 | (기재됨)
Edinburgh Pig Behaviour (Kaggle) | kaggle.com/datasets/jackbyte/edinburgh-pig-behaviour-annotated · CC BY-NC 4.0 — 분석에만 사용, 원자료 미포함 | ____
Multi-view Pig Posture (Kaggle Competition) | kaggle.com/competitions/multi-view-pig-posture-recognition | ____
돼지 탐지 bbox (Kaggle, 8개 소스 45,611 bbox) | (사용한 데이터셋 페이지 URL·라이선스 표기) | ____
스마트팜코리아 양돈 통계 (연도별·월별) | (내려받은 페이지 URL) | ____
```

⚠️ AI Hub URL 은 71763 행에 쓰신 형식을 그대로 따랐습니다. **브라우저에서
각 링크가 실제로 열리는지 한 번씩 확인**하고 넣으세요 — 링크 오기재도
오기재입니다.

```
■ 문헌·법령
축산법 시행령 [별표 1] 가축사육시설의 규모 (개정 2025. 3. 25.)
『돼지 기르기』, 농촌진흥청, 2020 — 생리 상수·사양 기준 (인용 쪽수: ____)
국립축산과학원 축사 환경 기준 — NH₃·H₂S·단계별 적온·습도 (자료명: ____)
Belstra, B. et al. (2002) — 하절기 발정 지속·배란 지연 (서지 확인: ____)
축산물이력제 적정 사육면적 계산기 (mtrace.go.kr) — 밀도 산식 대조
```

```
■ 데이터 이용 고지
AI Hub·Kaggle·스마트팜코리아 원자료와 외부 농장 영상은 제출물에 포함하지
않았으며, 기재된 수치는 전부 이들로부터 산출한 결과값이다. 농장 식별 정보는
어디에도 포함하지 않았다. 두당 마진의 단가 인자(지육률·지육가·FCR·비사료비)는
국내 통상 수준의 어림값으로 전부 조정 가능한 인자이며, 금액은 개선 순서
판단용이다.
```

---

## ⑤ 본문 잔여 수정 4곳 (찾아 바꾸기)

| 쪽 | 찾기 | 바꾸기 |
|---|---|---|
| p6 | 이 항등식이 466농장에서 | 이 항등식이 466 농장-연에서 |
| p5 각주 | 게체폭 | 개체폭 |
| p14 | 2976만 | 2,976만 |
| p16 | 이후 이후 행동 어휘는 | 이후 행동 어휘는 |

---

## ⑥ (선택) p5 622 절 — 한 문장 보강

622를 "라벨로 상한만 쟀다"에서 "학습까지 했다"로 격상할 수 있습니다.
"bbox 라벨은 71471과 합쳐 ... 탐지 단계학습에 쓰인다." 뒤에:

```
폴리곤 라벨로는 행동 15종 인스턴스 분할기를 학습해 외부 영상 관통(5장)의 행동 추론에 사용했다.
```

---

## ⑦ 첨부물 폴더에 추가 — 가중치 (이제 넣을 수 있음)

세 가중치 전부 자체 학습으로 확인됐으므로 첨부 가능:

```
07_가중치/
├─ pig_yolo.pt        (24,441,447 바이트)
│    SHA256 e4ab78032da3249a897656cffc35fee93822ec7182eef2a68a634cfafe47cdd2
└─ end2end.onnx       (Mask R-CNN 15클래스 · mmdeploy 1.3.1 FP32)
     SHA256 ____  ← 로컬에서: Get-FileHash end2end.onnx -Algorithm SHA256
```

`00_먼저읽기.txt` 에 한 줄 추가:

```
07_가중치     탐지·행동 모델의 학습 가중치입니다. 둘 다 본 과제에서 직접
              학습했으며 제3자 모델을 차용하지 않았습니다.
```
